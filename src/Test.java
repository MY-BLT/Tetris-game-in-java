
/**
 * This class represents a simple Tetris game where blocks fall down the game
 * grid.
 * The grid size is 10x20 represented by a boolean 2D array.
 * The game will lock blocks when they reach the bottom, clear full rows, and
 * end when a block reaches the top of the grid.
 * Blocks are represented by the 'X' character, filled cells by '■', and empty
 * cells by a space ' '.
 */
public class Test {
    public static void main(String[] args) {
        String context = "You can eat water too! ";
        context += new String(Character.toChars(0x1F349));
        System.out.println(context + "😊😘");
    }

}
